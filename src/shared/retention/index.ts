// Barrel: the retention seam every context registers its table sweeps into. Pure enough to import
// from anywhere — the port is an interface and the registry holds no infrastructure.
export * from './retention-sweep.port';
export * from './retention-sweep.registry';
export * from './retention.module';
