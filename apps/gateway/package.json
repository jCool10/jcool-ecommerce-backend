{
  "name": "@jcool/gateway",
  "version": "0.0.0",
  "private": true,
  "license": "UNLICENSED",
  "description": "Caddy in front of the services: for now only the private load balancer over the id-service replicas",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test:system": "vitest run --config ./test/vitest-system.config.mts"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "testcontainers": "^12.1.0",
    "typescript": "^5.7.3",
    "vitest": "^5.0.0"
  }
}
