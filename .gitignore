# Dependencies
/node_modules

# Build output
/dist
/build

# Coverage / test artifacts
/coverage
/.nyc_output

# Environment (never commit real secrets; the *.example files are the committed templates)
.env
.env.*
!.env.example
!.env.user.example

# Local observability secret (Phase 5): the real Prometheus scrape token, mounted into the
# Prometheus container. Must match the app's METRICS_TOKEN. Only the .example template is committed.
infra/prometheus/secrets/metrics-token

# Logs
*.log
npm-debug.log*
logs

# Editor / OS
.DS_Store
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?


!README.md
plans/
docs/

.agentkit/
.claude/
*.txt

bruno/